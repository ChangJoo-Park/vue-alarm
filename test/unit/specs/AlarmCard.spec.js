import Vue from 'vue'
import AlarmCard from 'src/components/Shared/AlarmCard'

describe('AlarmCard', () => {
  it('should return true', () => {
    console.log('should return true', Vue, AlarmCard)
    assert.equal('hello', 'hello')
  })
})
